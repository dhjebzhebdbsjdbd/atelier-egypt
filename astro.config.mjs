// @ts-check
import { defineConfig } from 'astro/config';

// Static site — Astro ships zero JS by default.
// When you buy a domain later, set `site` to it (e.g. 'https://atelier-egypt.com').
export default defineConfig({
  site: 'https://atelier-egypt.com',
});
